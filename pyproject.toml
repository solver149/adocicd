[tool.poetry]
name = "adocicd"
version = "0.1.0"
description = "azure devops ci cd testing"
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
